-5장-

값을 리턴했을때 call by value
(슬라이드 5)
포인터나 배열을 이용해서 값을 바꿔야 한다.(swap)  ***중요***
각 값을 스택을 경유해서 전달해준다.(슬라이드 6 내용 해석)
c++에서는 등호의 왼쪽과 오른쪽의 구조가 같을때 한번에 복사가 되는것을 허용한다.
(C에서는 데이터의 사이즈가 4bit가 넘으면 안되는 제약이 있어서 안됨)

reference = 참조
주소만 엑세스 할 용도로 만들어놨다 - 참조변수

슬라이드(21)
자료형이 일치하지 않아 오류가 난다.(int인데 리턴 된 값이 실수형(/로 나눠서)이라 오류가 난다.

최대 double형까지만 return 가능하다.(데이터가 8*8 = 64bit)
find : 공간 참조 함수

-6장-
static은 프로그램이 시작할 때, non-static은 객체가 생성될 때 함께 생성  ***중요***