8장

객체 사이에 상속은 없다.
cview
업 캐스팅 - '위로 찾아간다' 라는 것. 상속받은 객체들을 가리키는것이 개념.
다운 캐스팅 - 큰 것에다 작은 것을 넣음
아버지 클래스가 되려면 기본 생성자가 있어야한다.
일반적으로 protected를 쓰기보단 public을 사용해라. (보안을 중요시 하는것이 아니면)

9장

virtual : 호출되지 않도록 강제한다.
오버라이딩 = 함수 재정의 = 다형성
함수 재정의를 하면(virtual) 자식 클래스의 함수만 실행되니 이쪽만 수정하면 된다. (슬라이드 7)

10장(사용하는 사람만 사용함)
(슬라이드 32) .at : 이동명령.

11장은 폭 정렬과 세팅하는 방법 정도 알아두기.

(이것저것)
boot loader(리눅스) = BIOS(윈도우)
부트로더 = OS를 적재함.

우리가 앞으로 해야 할 것 : 부트로더를 만들고 리눅스 커널을 구축하고 bsp만들기
부트로더는 메모리에서 만들다가 다 만들면 메모리에서 사라진다.

***MFC에서는 순서가 중요하다***
연결을 먼저하고 코딩을 하는것이 좋다.