함수의 중복 = overloading


2장 ppt

데이터베이스는 기본적으로 문자열로 처리한다.
숫자는 더블형, 문자는 문자열형

(슬라이드16) 스페이스바, 엔터, 탭으로 여러 값 입력을 구분한다. 
코드의 재사용성 때문에 하나의 헤더파일에 하나의 클래스를 선언하는 것이 좋다.


3장 ppt 

소멸자는 생성의 역순으로 닫힌다.

class 접근지정 ----*가장 중요*----
변수들은 private
함수들은 public 에 넣는다.

(슬라이드 51) ***매우 중요***

4장 ppt
(슬라이드 15)
delete는 삭제하는게 아니라 연결을 끊는것.

this *중요*