C++ 의 최대 장점 중 하나는 추상화

C++는 자원을 효율적으로 관리하는것이 중요
C++는 명시적인 것을 좋아함.(단 void는 명시적이지 않음)

void 포인터는 형태가 계속 바뀜.

C++ 의 장점에 대한 질문 중 가장 중요한 것 >> 다형성(Polymorphism)

다형성(Polymorphism)
- ... 형태가 여러개
-  ..상속 .. 재정의 .. 형태 -> 객체 -> 객체s
- .. pointer를 만든 이유?
>     ... 부모
      ->       <-	서로 부모를 가리킴.
> ...자식1 ...자식2
>부모 클래스를 통해서 자식 클래스를 통제 할 수 있음.

int 같은것도 하나의 클래스.

define 보다는 const를 사용하라.
vector는 초기화를 시켜줘야한다. ( clear() )